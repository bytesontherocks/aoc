#include <vector>
#include <cinttypes>

namespace measurements {

    uint32_t getNumberOfIncreases(const std::vector<uint32_t>& v);
    uint32_t getNumberOfIncreasesWindow(const std::vector<uint32_t>& v);
}