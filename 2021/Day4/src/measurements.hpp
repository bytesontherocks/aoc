#include <utility>

#include <cinttypes>
#include <iostream>
#include <fstream>
namespace measurements {

    int64_t calculateFirstExercice(std::ifstream& input);
    int64_t calculateSecondExercice(std::ifstream& input);
}