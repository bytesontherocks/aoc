#include <utility>

#include <cinttypes>
#include <iostream>
#include <vector>
#include <fstream>

namespace measurements {

    uint64_t calculateFirstExercice(std::ifstream& input);
    uint64_t calculateSecondExercice(std::ifstream& input);
}