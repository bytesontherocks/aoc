#include <utility>

#include <cinttypes>
#include <iostream>
#include <vector>

namespace measurements {

    uint64_t calculateFirstExercice(std::vector<size_t> v, const size_t days);
    uint64_t calculateSecondExercice(std::vector<size_t> v, const size_t days);
}